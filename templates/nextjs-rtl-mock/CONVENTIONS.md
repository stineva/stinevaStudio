# قوانین توسعه

- همه صفحات باید در مسیر `app/` باشد.
- اجزای مشترک در `components/` و فیچرها در `features/` قرار گیرند.
- داده‌های ساختگی از `mock/` و لایه API ساختگی از `lib/mockApi.ts`.
- رابط کاربری راست‌به‌چپ و فارسی باقی بماند.
